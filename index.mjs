// index.mjs — dsh-vietnamese-lang Host-side entry
const name = 'dsh-vietnamese-lang'
const inject = []

function apply() {
  // Pure client plugin: all translations run in the client-side ModuleLoader.
}

export { apply, inject, name }
